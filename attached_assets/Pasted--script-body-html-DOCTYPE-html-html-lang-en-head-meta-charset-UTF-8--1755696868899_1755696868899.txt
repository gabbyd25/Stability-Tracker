</script>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stability Testing Tracker</title>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #a855f7 0%, #ec4899 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #d946ef 0%, #f97316 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 300;
        }

        .header p {
            opacity: 0.9;
            font-size: 1.1em;
        }

        .main-content {
            padding: 30px;
        }

        .input-section {
            background: #f8fafc;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 30px;
            border: 2px dashed #e2e8f0;
        }

        .input-row {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            align-items: end;
        }

        .input-group {
            flex: 1;
        }

        .input-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
            color: #374151;
        }

        .input-group input {
            width: 100%;
            padding: 12px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .input-group input:focus {
            outline: none;
            border-color: #d946ef;
            box-shadow: 0 0 0 3px rgba(217, 70, 239, 0.1);
        }

        .btn-primary {
            background: linear-gradient(135deg, #d946ef 0%, #ec4899 100%);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(217, 70, 239, 0.3);
        }

        .tasks-section {
            margin-top: 30px;
        }

        .tasks-header {
            display: flex;
            justify-content: between;
            align-items: center;
            margin-bottom: 20px;
        }

        .tasks-header h2 {
            color: #1f2937;
            font-size: 1.8em;
            font-weight: 600;
        }

        .task-filters {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .filter-btn {
            padding: 8px 16px;
            border: 2px solid #e5e7eb;
            background: white;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 500;
        }

        .filter-btn.active,
        .filter-btn:hover {
            background: #d946ef;
            color: white;
            border-color: #d946ef;
        }

        .tasks-grid {
            display: grid;
            gap: 15px;
        }

        .task-card {
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            padding: 20px;
            transition: all 0.3s;
            border-left: 4px solid #e5e7eb;
        }

        .task-card:hover {
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
            transform: translateY(-2px);
        }

        .task-card.weekly {
            border-left-color: #ec4899;
        }

        .task-card.ft-thaw {
            border-left-color: #d946ef;
        }

        .task-card.ft-test {
            border-left-color: #a855f7;
        }

        .task-card.overdue {
            border-left-color: #ef4444;
            background: #fef2f2;
        }

        .task-card.due-today {
            border-left-color: #f59e0b;
            background: #fffbeb;
        }

        .task-name {
            font-size: 1.1em;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 8px;
        }

        .task-details {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .task-date {
            color: #6b7280;
            font-size: 0.9em;
        }

        .task-type {
            padding: 4px 12px;
            border-radius: 16px;
            font-size: 0.8em;
            font-weight: 600;
        }

        .type-weekly {
            background: #fce7f3;
            color: #be185d;
        }

        .type-ft-thaw {
            background: #f3e8ff;
            color: #7c3aed;
        }

        .type-ft-test {
            background: #ede9fe;
            color: #6b21a8;
        }

        .task-actions {
            display: flex;
            gap: 10px;
        }

        .btn-complete {
            background: #ec4899;
            color: white;
            border: none;
            padding: 6px 12px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 0.9em;
            transition: background 0.3s;
        }

        .btn-complete:hover {
            background: #db2777;
        }

        .btn-email {
            background: #a855f7;
            color: white;
            border: none;
            padding: 6px 12px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 0.9em;
            transition: background 0.3s;
        }

        .btn-email:hover {
            background: #9333ea;
        }

        .completed {
            opacity: 0.6;
            text-decoration: line-through;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(135deg, #a855f7 0%, #ec4899 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
        }

        .stat-number {
            font-size: 2em;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .stat-label {
            opacity: 0.9;
            font-size: 0.9em;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #6b7280;
        }

        .empty-state h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            color: #374151;
        }

        @media (max-width: 768px) {
            .input-row {
                flex-direction: column;
            }
            
            .header h1 {
                font-size: 2em;
            }
            
            .main-content {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🧪 Stability Testing Tracker</h1>
            <p>Automated reminders for weekly stability checks and freeze/thaw cycles</p>
        </div>

        <div class="main-content">
            <div class="input-section">
                <h3 style="margin-bottom: 20px; color: #374151;">Add New Product</h3>
                <div class="input-row">
                    <div class="input-group">
                        <label for="productName">Product Name</label>
                        <input type="text" id="productName" placeholder="e.g., Product XYZ">
                    </div>
                    <div class="input-group">
                        <label for="startDate">Start Date</label>
                        <input type="date" id="startDate">
                    </div>
                    <div class="input-group">
                        <label for="emailAddress">Email Address</label>
                        <input type="email" id="emailAddress" placeholder="your@email.com">
                    </div>
                    <button class="btn-primary" onclick="addProduct()">Create Tasks</button>
                    <button class="btn-primary" onclick="exportToOutlook()" style="background: linear-gradient(135deg, #0078d4 0%, #106ebe 100%);">📅 Export to Outlook</button>
                </div>
            </div>

            <div class="stats" id="stats"></div>

            <div class="tasks-section">
                <div class="tasks-header">
                    <h2>All Tasks</h2>
                </div>
                
                <div class="task-filters">
                    <button class="filter-btn active" onclick="filterTasks('all')">All</button>
                    <button class="filter-btn" onclick="filterTasks('weekly')">Weekly</button>
                    <button class="filter-btn" onclick="filterTasks('ft')">Freeze/Thaw</button>
                    <button class="filter-btn" onclick="filterTasks('due')">Due Today</button>
                    <button class="filter-btn" onclick="filterTasks('overdue')">Overdue</button>
                </div>

                <div class="tasks-grid" id="tasksGrid">
                    <div class="empty-state">
                        <h3>No tasks yet</h3>
                        <p>Add your first product above to create stability testing tasks</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let tasks = [];
        let currentFilter = 'all';

        function addProduct() {
            const productName = document.getElementById('productName').value.trim();
            const startDate = document.getElementById('startDate').value;
            const emailAddress = document.getElementById('emailAddress').value.trim();

            if (!productName || !startDate || !emailAddress) {
                alert('Please fill in all fields');
                return;
            }

            const newTasks = generateTasks(productName, startDate, emailAddress);
            tasks = [...tasks, ...newTasks];
            
            // Clear inputs
            document.getElementById('productName').value = '';
            document.getElementById('emailAddress').value = '';
            
            updateDisplay();
            
            alert(`Created ${newTasks.length} tasks for ${productName}!`);
        }

        function generateTasks(productName, startDate, email) {
            const start = new Date(startDate);
            const newTasks = [];

            // Weekly tasks
            const weeklySchedule = [
                { name: 'Initial', days: 0 },
                { name: 'Week 1', days: 7 },
                { name: 'Week 2', days: 14 },
                { name: 'Week 3', days: 21 },
                { name: 'Week 4', days: 28 },
                { name: 'Week 8', days: 56 },
                { name: 'Week 13', days: 91 }
            ];

            weeklySchedule.forEach(week => {
                const dueDate = new Date(start);
                dueDate.setDate(start.getDate() + week.days);
                
                newTasks.push({
                    id: Date.now() + Math.random(),
                    name: `Stability - ${productName} ${week.name}`,
                    productName,
                    dueDate: dueDate.toISOString().split('T')[0],
                    type: 'weekly',
                    email,
                    completed: false,
                    cycle: week.name
                });
            });

            // Freeze/Thaw cycles
            const ftCycles = [
                { cycle: 1, thawDay: 1, testDay: 2 },
                { cycle: 2, thawDay: 3, testDay: 4 },
                { cycle: 3, thawDay: 5, testDay: 6 }
            ];

            ftCycles.forEach(ft => {
                // Thaw task
                const thawDate = new Date(start);
                thawDate.setDate(start.getDate() + ft.thawDay);
                
                newTasks.push({
                    id: Date.now() + Math.random(),
                    name: `Stability - ${productName} F/T Cycle ${ft.cycle} [Thaw 24 hrs]`,
                    productName,
                    dueDate: thawDate.toISOString().split('T')[0],
                    type: 'ft-thaw',
                    email,
                    completed: false,
                    cycle: `Cycle ${ft.cycle}`
                });

                // Test task
                const testDate = new Date(start);
                testDate.setDate(start.getDate() + ft.testDay);
                
                newTasks.push({
                    id: Date.now() + Math.random(),
                    name: `Stability - ${productName} F/T Cycle ${ft.cycle} [Test 24 hrs]`,
                    productName,
                    dueDate: testDate.toISOString().split('T')[0],
                    type: 'ft-test',
                    email,
                    completed: false,
                    cycle: `Cycle ${ft.cycle}`
                });
            });

            return newTasks;
        }

        function filterTasks(filter) {
            currentFilter = filter;
            
            // Update filter buttons
            document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            
            updateDisplay();
        }

        function getFilteredTasks() {
            const today = new Date().toISOString().split('T')[0];
            
            switch(currentFilter) {
                case 'weekly':
                    return tasks.filter(task => task.type === 'weekly');
                case 'ft':
                    return tasks.filter(task => task.type.startsWith('ft'));
                case 'due':
                    return tasks.filter(task => task.dueDate === today && !task.completed);
                case 'overdue':
                    return tasks.filter(task => task.dueDate < today && !task.completed);
                default:
                    return tasks;
            }
        }

        function updateDisplay() {
            updateStats();
            updateTasksGrid();
        }

        function updateStats() {
            const today = new Date().toISOString().split('T')[0];
            const total = tasks.length;
            const completed = tasks.filter(t => t.completed).length;
            const dueToday = tasks.filter(t => t.dueDate === today && !t.completed).length;
            const overdue = tasks.filter(t => t.dueDate < today && !t.completed).length;

            document.getElementById('stats').innerHTML = `
                <div class="stat-card">
                    <div class="stat-number">${total}</div>
                    <div class="stat-label">Total Tasks</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">${completed}</div>
                    <div class="stat-label">Completed</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">${dueToday}</div>
                    <div class="stat-label">Due Today</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">${overdue}</div>
                    <div class="stat-label">Overdue</div>
                </div>
            `;
        }

        function updateTasksGrid() {
            const filteredTasks = getFilteredTasks();
            const grid = document.getElementById('tasksGrid');
            
            if (filteredTasks.length === 0) {
                grid.innerHTML = `
                    <div class="empty-state">
                        <h3>No tasks found</h3>
                        <p>Try a different filter or add a new product</p>
                    </div>
                `;
                return;
            }

            // Sort by due date
            filteredTasks.sort((a, b) => new Date(a.dueDate) - new Date(b.dueDate));
            
            const today = new Date().toISOString().split('T')[0];
            
            grid.innerHTML = filteredTasks.map(task => {
                const isOverdue = task.dueDate < today && !task.completed;
                const isDueToday = task.dueDate === today && !task.completed;
                
                let cardClass = `task-card ${task.type}`;
                if (isOverdue) cardClass += ' overdue';
                if (isDueToday) cardClass += ' due-today';
                if (task.completed) cardClass += ' completed';

                const typeLabels = {
                    'weekly': 'Weekly Stability',
                    'ft-thaw': 'F/T Thaw',
                    'ft-test': 'F/T Test'
                };

                return `
                    <div class="${cardClass}">
                        <div class="task-name">${task.name}</div>
                        <div class="task-details">
                            <div class="task-date">Due: ${new Date(task.dueDate).toLocaleDateString()}</div>
                            <div class="task-type type-${task.type}">${typeLabels[task.type]}</div>
                        </div>
                        <div class="task-actions">
                            ${!task.completed ? `
                                <button class="btn-complete" onclick="completeTask('${task.id}')">
                                    ✓ Complete
                                </button>
                                <button class="btn-email" onclick="sendReminder('${task.id}')">
                                    📧 Send Reminder
                                </button>
                            ` : '<span style="color: #ec4899; font-weight: 600;">✓ Completed</span>'}
                        </div>
                    </div>
                `;
            }).join('');
        }

        function completeTask(taskId) {
            const task = tasks.find(t => t.id == taskId);
            if (task) {
                task.completed = true;
                updateDisplay();
            }
        }

        function sendReminder(taskId) {
            const task = tasks.find(t => t.id == taskId);
            if (!task) return;

            // Create email content
            const subject = `Stability Task Due: ${task.name}`;
            const body = `Hi,

This is a reminder that you have a stability testing task due:

Task: ${task.name}
Product: ${task.productName}
Due Date: ${new Date(task.dueDate).toLocaleDateString()}
Type: ${task.type.replace('ft-', 'F/T ').replace('-', ' ').toUpperCase()}

Please complete this task and mark it as done in your tracker.

Best regards,
Stability Testing System`;

            // Open default email app with pre-filled content
            const emailUrl = `mailto:${task.email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
            window.open(emailUrl);
            
            alert(`📧 Email opened in your default email app!\n\nSending to: ${task.email}`);
        }

        function exportToOutlook() {
            if (tasks.length === 0) {
                alert('Please add some products and tasks first!');
                return;
            }

            // Create ICS (iCalendar) format for Outlook
            let icsContent = `BEGIN:VCALENDAR
VERSION:2.0
PRODID:-//Stability Tracker//EN
CALSCALE:GREGORIAN
METHOD:PUBLISH
X-WR-CALNAME:Stability Testing
X-WR-TIMEZONE:America/New_York
X-WR-CALDESC:Stability testing reminders for all products
`;

            tasks.forEach(task => {
                const dueDate = new Date(task.dueDate);
                
                // Set reminder time to 9:00 AM on due date
                const startDateTime = new Date(dueDate);
                startDateTime.setHours(9, 0, 0, 0);
                
                const endDateTime = new Date(startDateTime);
                endDateTime.setHours(10, 0, 0, 0); // 1 hour duration

                // Format dates for ICS (YYYYMMDDTHHMMSS)
                const formatICSDate = (date) => {
                    return date.toISOString().replace(/[-:]/g, '').replace(/\.\d{3}/, '');
                };

                const startICS = formatICSDate(startDateTime);
                const endICS = formatICSDate(endDateTime);
                
                // Create unique ID
                const uid = `stability-${task.id}@stability-tracker`;
                
                // Task description with details
                const description = `Stability Testing Task\\n\\nProduct: ${task.productName}\\nTask: ${task.name}\\nType: ${task.type.replace('ft-', 'F/T ').replace('-', ' ').toUpperCase()}\\nCycle: ${task.cycle}\\n\\nPlease complete this stability check and update your records.`;

                // Add event to ICS
                icsContent += `
BEGIN:VEVENT
UID:${uid}
DTSTART:${startICS}
DTEND:${endICS}
SUMMARY:${task.name}
DESCRIPTION:${description}
LOCATION:Laboratory
STATUS:CONFIRMED
SEQUENCE:0
BEGIN:VALARM
TRIGGER:-PT15M
DESCRIPTION:Stability testing reminder
ACTION:DISPLAY
END:VALARM
END:VEVENT`;
            });

            icsContent += `
END:VCALENDAR`;

            // Create and download the file
            const blob = new Blob([icsContent], { type: 'text/calendar;charset=utf-8' });
            const link = document.createElement('a');
            link.href = window.URL.createObjectURL(blob);
            link.download = 'stability-testing-calendar.ics';
            
            // Trigger download
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);

            // Show success message with instructions
            alert(`📅 Calendar file downloaded!

TO IMPORT INTO OUTLOOK:
1. Find the downloaded file: "stability-testing-calendar.ics"
2. Double-click it (opens in Outlook automatically)
3. Click "Save & Close" to add all events
4. Your ${tasks.length} stability tasks are now in Outlook calendar!

FEATURES ADDED:
✅ All tasks as calendar events at 9:00 AM
✅ 15-minute email reminders before each task
✅ Full task details in each event
✅ Proper categories and descriptions`);
        }

        // Set today's date as default
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('startDate').value = new Date().toISOString().split('T')[0];
        });

        // Initial display update
        updateDisplay();
        // Check for due tasks on load
        window.addEventListener('load', () => {
            const today = new Date().toISOString().split('T')[0];
            const dueTasks = tasks.filter(t => t.dueDate === today && !t.completed);
            
            if (dueTasks.length > 0) {
                alert(`You have ${dueTasks.length} task(s) due today!`);
            }
        });