# Stability Tracker - Environment Variables
# Copy this file to .env and customize as needed

# Node.js Server Port (optional, defaults to 5000)
# PORT=5000

# Django Settings (optional overrides)
# DJANGO_SECRET_KEY=your-secret-key-here
# DJANGO_DEBUG=True
# DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite is used by default, no configuration needed)
# DB_NAME=db.sqlite3

# NOTES:
# - The Django backend runs on port 7834 by default
# - The React frontend runs on port 5173 (dev) or 5000 (production)
# - No API keys are required for basic functionality
